#include "clonal_tree_refiner.hpp"

namespace antevolo {
    /*
    void ClonalTreeRefiner::FillAaSHMMap() {
        VERIFY(false);
    }

    bool ClonalTreeRefiner::ConflictsExist() {
        return refined_tree_.NumEdges() < clonal_graph_.NumVertices() - 1;
    }

    ClonalTreeRefiner::ClonalEdge ClonalTreeRefiner::GetMaximalConflictingEdge() {
        // select conflicting edge with maximal weight
    }

    void ClonalTreeRefiner::ResolveConflictingGroup(ClonalEdge selected_edge) {
        // add selected edge to the tree
        // remove other conflicing edges from the map
    }
    */
}